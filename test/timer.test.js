const assert = require('assert');
const timer = require('../time.js');

it('takes five', done => {
     // test goes here
    const start = new Date();
    const time = timer(start);
     
}).timeout(7000);
